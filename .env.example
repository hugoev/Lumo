# Database Configuration
POSTGRES_DB=lumo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_db_password_here

# JWT Configuration
JWT_KEY=your_super_secret_jwt_key_at_least_32_characters_long
JWT_ISSUER=https://localhost:5001
JWT_AUDIENCE=https://localhost:5001

# Production Domain (for production deployment)
PRODUCTION_DOMAIN=https://yourdomain.com

# Database Connection String (automatically constructed from above variables)
# ConnectionStrings__DefaultConnection will be built from the above variables
